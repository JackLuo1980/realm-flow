# 运维连接信息

> 更新时间：2026-03-18
> 用途：记录节点与面板相关服务器登录信息，方便后续自动化导出与巡检。

## 中转服务器（Leikwan）

- 主机名：`leikwan.zlib4.eu.org`
- IP：动态 DDNS
- SSH 端口：`22`
- SSH 用户：`root`
- SSH 密码：`Lpsz800203`
- 备注：主中转与策略路由服务器（`gost` + `dual-route`）。

## 备用中转服务器（腾讯云）

- IP：`111.229.215.107`
- SSH 端口：`22`
- SSH 用户：`root`
- SSH 密码：`Pk7enmXt06fr`
- 备注：备用中转节点。

## Komari 服务器

- 名称：`vps.ja.edu.kg`
- IP：`159.54.184.3`
- SSH 端口：`22`
- SSH 用户：`root`
- SSH 密码：`lpsz800203`
- 备注：运行 `komari` 容器，数据挂载目录为 `/home/docker/komari`。
- 运行注意：
  - 出现“删除提示成功但实际未删除”时，先查磁盘空间：`df -h`。
  - 禁止恢复旧的 10 分钟一次 DB 备份任务，避免再次打满根分区。
  - 若目标机 443 出站受限，优先使用 “RFC 中转 + SNI Forward + nftables OUTPUT DNAT” 标准方案。

## Panel 服务器（pande）

- IP：`185.117.73.38`
- SSH 用户：`root`
- SSH 密码：`I1g5n4GA3R9eBhCv5t`
- 备注：运行 `vite-frontend`、`springboot-backend`、`gost-mysql` 容器。

## Panel 数据库连接（从服务器环境读取）

- DB_NAME：`BoFfpNBeXA8Yjt24`
- DB_USER：`yvKuBRNUNUssGTtl`
- DB_PASSWORD：`7mPRem0aHTi6n8KO`
- 来源：`/root/.env`（pande 服务器）

## SSH 快捷命令

```bash
# Leikwan 主中转
sshpass -p 'Lpsz800203' ssh -o StrictHostKeyChecking=no root@leikwan.zlib4.eu.org

# 腾讯云备用中转
sshpass -p 'Pk7enmXt06fr' ssh -o StrictHostKeyChecking=no root@111.229.215.107

# Komari 监控服务器
sshpass -p 'lpsz800203' ssh -o StrictHostKeyChecking=no root@159.54.184.3
```
